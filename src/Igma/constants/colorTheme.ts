export interface IColorTheme {
  sideMenu: {
    backgroundColor: string;
    buttonBackground: string;
    fontColor: string;
  };
  chart: {
    backgroundColor: string;
  };
}

export const ultraViolet = {
  sideMenu: {
    backgroundColor: "#3e1d95",
    buttonBackground: "#a092cb",
    fontColor: "#fff",
  },
  chart: {
    backgroundColor: "#3e1d95",
  },
};
